import React from 'react';\nimport { StyleSheet, View, Text, ViewStyle } from 'react-native';\nimport { BlurView } from 'expo-blur';\nimport { DARK_LUXURY_COLORS } from '@/utils/colors';\n\ninterface GlassmorphCardProps {\n  children: React.ReactNode;\n  style?: ViewStyle;\n  intensity?: number;\n}\n\nexport const GlassmorphCard: React.FC<GlassmorphCardProps> = ({\n  children,\n  style,\n  intensity = 80,\n}) => {\n  return (\n    <BlurView intensity={intensity} style={[styles.container, style]}>\n      {children}\n    </BlurView>\n  );\n};\n\nconst styles = StyleSheet.create({\n  container: {\n    backgroundColor: DARK_LUXURY_COLORS.glass,\n    borderRadius: 24,\n    overflow: 'hidden',\n    borderWidth: 1,\n    borderColor: DARK_LUXURY_COLORS.whiteOpacity10,\n  },\n});\n